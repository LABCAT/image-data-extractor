import './components/ImageDataExtractor.ts';
const app = document.getElementById('app');
const component = document.createElement('image-data-extractor');

if(app) {
    app.appendChild(component);
}
